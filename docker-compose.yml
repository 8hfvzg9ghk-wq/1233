services:
  hakimi-pomodoro:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
    restart: always
    container_name: hakimi-pomodoro-app

  # 可选：添加一个简单的HTTP服务器作为替代
  # 如果不需要Nginx的复杂配置，可以使用这个服务
  # simple-server:
  #   image: python:3.9-alpine
  #   ports:
  #     - "8081:8000"
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: python -m http.server 8000
  #   restart: always
  #   container_name: hakimi-pomodoro-simple